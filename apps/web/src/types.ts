// Re-export shared types from their module sources.
// Add new types inside the relevant module's types/ folder, not here.
export type { AuthUser, UserRole } from '@web/modules/auth/types/auth.types';
